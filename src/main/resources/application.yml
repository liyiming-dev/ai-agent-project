spring:
  application:
    name: yu-ai-code-mother-backend
server:
  port: 8120
  servlet:
    context-path: /api

# springdoc-openapi????
springdoc:
  group-configs:
    - group: 'default'
      paths-to-match: '/**'
      packages-to-scan: com.yiming.aiagentproject.controller
# knife4j???????????????
knife4j:
  enable: true
  setting:
    language: zh_cn